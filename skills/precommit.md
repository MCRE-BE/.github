# Pre-commit (prek)

We utilize `prek` to orchestrate our pre-commit hook executions.

- **Run all pre-commit hooks:** `prek run --all-files`

**Rule of Verification:** If a pre-commit hook fails, you must attempt to fix the errors before proceeding.
